{{-- <div>
    @if ($payment)
        <button type="button" id="pay-button" class="btn btn-primary btn-checkout rounded-pill mt-3">
            <span>Pay Now</span>
        </button>
    @endif
</div> --}}
